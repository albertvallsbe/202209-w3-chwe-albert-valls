import Component from "../Component/Component.js";

class Main extends Component {
  constructor(parentElement: HTMLElement) {
    super(parentElement, "main-body", "main");
  }

  render() {
    super.render();
  }
}

export default Main;
