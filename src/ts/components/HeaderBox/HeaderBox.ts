import Component from "../Component/Component.js";

class HeaderBox extends Component {
  constructor(parentElement: HTMLElement) {
    super(parentElement, "main-header3", "header");
  }

  render() {
    super.render();
  }
}

export default HeaderBox;
